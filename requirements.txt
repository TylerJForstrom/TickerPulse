# Core worker dependencies — lightweight, run anywhere (laptop, CI).
# Heavy ML extras (FinBERT, sentence-transformers) live in requirements-ml.txt
# and are only installed in the GitHub Actions worker.
requests>=2.34.2
pandas>=2.1
numpy>=1.26
scikit-learn>=1.9.0
python-dotenv>=1.2.2
yfinance>=1.5.2
feedparser>=6.0.14
psycopg[binary]>=3.1
pytest>=8.0
