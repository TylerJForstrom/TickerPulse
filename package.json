{
  "name": "tickerpulse",
  "version": "0.1.0",
  "private": true,
  "description": "Live social sentiment & trend radar for the stock market",
  "scripts": {
    "build": "npm --prefix dashboard ci && npm --prefix dashboard run build",
    "dev": "npm --prefix dashboard run dev"
  }
}
